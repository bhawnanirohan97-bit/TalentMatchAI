DATABASE_URL="postgresql://user:password@host:5432/talentmatch?sslmode=require"
# Set to use the OpenAI-backed AI adapter (extraction/interpretation). When
# unset, a deterministic development adapter is used instead.
# OPENAI_API_KEY=""
# Session signing secret. Generate with: openssl rand -base64 32
# SESSION_SECRET=""
